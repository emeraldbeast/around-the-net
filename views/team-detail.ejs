<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= team.name %> | Team Details
    </title>
    <link rel="icon" href="/images/around-the-net-logo.png" type="image/png">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        window.addEventListener('load', () => {
            const loader = document.getElementById('page-loader');
            if (loader) loader.classList.add('opacity-0', 'pointer-events-none');
            const video = document.getElementById('tt-video');
            if (video) video.playbackRate = 0.5;
        });
    </script>
    <script defer src="/_vercel/insights/script.js"></script>
</head>

<body class="bg-zinc-900 font-sans">
    <div id="page-loader"
        class="fixed inset-0 bg-white z-[100] flex items-center justify-center transition-opacity duration-[2000ms]">
        <video id="tt-video" autoplay muted loop playsinline class="w-[500px] h-[500px]">
            <source src="/animations/tt-loader.mp4" type="video/mp4" />
            Your browser does not support the video tag.
        </video>
    </div>
    <div
        class="max-w-3xl mx-auto p-4 sm:p-6 bg-gradient-to-br from-zinc-700 via-zinc-600 to-red-50 rounded-lg shadow-lg mt-10 ring-2 ring-red-500">

        <div
            class="w-32 sm:w-40 h-32 sm:h-40 rounded-full bg-black overflow-hidden border-4 border-red-500 mx-auto mb-4 shadow-md">
            <img src="<%= team.logo %>" alt="<%= team.name %> Logo" class="w-full h-full object-scale-down" />
        </div>

        <h1 class="text-2xl sm:text-3xl font-bold text-center text-white mb-4">
            <%= team.name %>
        </h1>

        <div class="flex flex-col gap-4 text-white">
            <div class="bg-yellow-600 text-white p-4 rounded-md shadow-md w-full text-center">
                <p class="text-lg font-bold">
                    <%= team.owner %>
                </p>
                <p class="text-sm font-medium">Manager</p>
            </div>
            <div class="bg-yellow-600 text-white p-4 rounded-md shadow-md w-full text-center">
                <p class="text-lg font-bold">
                    <%= team.captain %>
                </p>
                <p class="text-sm font-medium">Captain</p>
            </div>
        </div>

        <hr class="my-6 border-t border-gray-400" />

        <% if (Array.isArray(team.players) && team.players.length> 1) { %>
            <div
                class="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6 bg-yellow-100/40 backdrop-blur-sm p-4 rounded-md shadow-sm border border-yellow-300">
                <% for (let i=1; i < team.players.length; i++) { %>
                    <div
                        class="max-w-[220px] w-full bg-yellow-200/60 backdrop-blur-sm px-4 py-2 rounded-md text-center text-gray-800 font-medium shadow border border-yellow-300 mx-auto">
                        <%= team.players[i] %>
                    </div>
                    <% } %>
            </div>
            <% } %>

    </div>

    <div class="text-center mt-6">
        <a href="/"
            class="inline-block bg-gray-200 hover:bg-gray-300 text-gray-800 font-semibold py-2 px-4 rounded transition">
            Go back to homepage
        </a>
    </div>
</body>

</html>